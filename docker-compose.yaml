version: "3.8"

services:
  app:
    build: ./app
    restart: unless-stopped
    # ports:
    #   - 3:$APP_DOCKER_PORT
  nginx:
    depends_on:
      - app
    build: ./nginx
    restart: always
    ports:
      - 80:80
    volumes:
      - ./test:/usr/share/nginx/html